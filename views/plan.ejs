<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plan Details</title>
</head>
<body>
    <header>
        <h1>Plan Details</h1>
        <a href="/plans">Back to All Plans</a>
        <a href="/logout">Logout</a> <!-- Link to logout -->
    </header>
    
    <section>
        <% if (plan) { %>
            <div class="plan-details">
                <h2><%= plan.title %></h2>
                <p><strong>Location:</strong> <%= plan.location %></p>
                <p><strong>Category:</strong> <%= plan.category %></p>
                <p><strong>Date:</strong> <%= plan.date.toISOString().split('T')[0] %></p>
                <p><strong>Time:</strong> <%= plan.time %></p>
                <p><strong>Description:</strong> <%= plan.description || 'No description available' %></p>
                <p><strong>Created By:</strong> <%= plan.created_by %></p>
                <p><strong>Status:</strong> <%= plan.status %></p>
                <p><strong>Created At:</strong> <%= new Date(plan.created_at).toLocaleString() %></p>

                <!-- Buttons for editing or deleting the plan -->
                <div class="actions">
                    <a href="/plans/edit/<%= plan.plan_id %>">Edit Plan</a>
                    <form action="/plans/<%= plan.plan_id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this plan?');">Delete Plan</button>
                    </form>
                </div>
            </div>
        <% } else { %>
            <p>No plan details available.</p>
        <% } %>
    </section>
</body>
</html>
