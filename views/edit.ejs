<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Plan</title>
</head>
<body>
    <header>
        <h1>Edit Plan</h1>
        <a href="/plans">Back to All Plans</a>
        <a href="/plans/<%= plan.plan_id %>">Back to Plan Details</a>
        <a href="/logout">Logout</a>
    </header>
    
    <section>
        <% if (plan) { %>
            <form action="/plans/<%= plan.plan_id %>?_method=PATCH" method="POST" class="edit-form">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" value="<%= plan.title %>" required>
                </div>

                <div class="form-group">
                    <label for="location">Location</label>
                    <input type="text" id="location" name="location" value="<%= plan.location %>" required>
                </div>

                <div class="form-group">
                    <label for="category">Category</label>
                    <input type="text" id="category" name="category" value="<%= plan.category %>" required>
                </div>

                <div class="form-group">
                    <label for="date">Date</label>
                    <input type="date" id="date" name="date" value="<%= plan.date.toISOString().split('T')[0] %>" required>
                </div>

                <div class="form-group">
                    <label for="time">Time</label>
                    <input type="time" id="time" name="time" value="<%= plan.time %>" required>
                </div>

                <div class="form-group">
                    <label for="description">Description</label>
                    <textarea id="description" name="description" rows="5"><%= plan.description %></textarea>
                </div>

                <div class="form-group">
                    <label for="created_by">Created By</label>
                    <input type="text" id="created_by" name="created_by" value="<%= plan.created_by %>" required>
                </div>

                <div class="form-actions">
                    <button type="submit">Update Plan</button>
                    <a href="/plans/<%= plan.plan_id %>" class="cancel-button">Cancel</a>
                </div>
            </form>
        <% } else { %>
            <p>No plan data available for editing.</p>
        <% } %>
    </section>
</body>
</html>
